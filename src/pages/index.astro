---
// Opt out of prerendering so the middleware can run and detect language at request time.
// In practice, the middleware always redirects before this page renders.
export const prerender = false;
---
